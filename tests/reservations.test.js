// Exemple de test Jest pour vérifier le fonctionnement de l'environnement de test

describe('Exemple de test', () => {
  it('doit vérifier que 1 + 1 = 2', () => {
    expect(1 + 1).toBe(2);
  });
}); 